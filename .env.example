# ⚠️ IMPORTANT: This is an EXAMPLE file only!
# Copy this to .env and change ALL values to your own secure credentials
# NEVER use these example values in production!

PORT=5000
MONGODB_URI=mongodb://localhost:27017/ecommerce
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_32_CHARACTER_STRING_IN_PRODUCTION
JWT_EXPIRE=7d
NODE_ENV=development

# Frontend URL (for payment callbacks)
FRONTEND_URL=http://localhost:8000

# ⚠️ CHANGE THESE! Default Super Admin Credentials (for first-time setup only)
# After first login, change these in your production environment!
SUPER_ADMIN_EMAIL=admin@yourdomain.com
SUPER_ADMIN_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# Google OAuth (Optional - get from Google Cloud Console)
# For local development:
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback
# For production, update GOOGLE_CALLBACK_URL to:
# https://your-backend-url.onrender.com/api/auth/google/callback
# And add this URL to Google Cloud Console Authorized redirect URIs

# Paystack Payment (Get from Paystack Dashboard)
PAYSTACK_SECRET_KEY=your_paystack_secret_key_here
PAYSTACK_PUBLIC_KEY=your_paystack_public_key_here

# Email Configuration (for password reset emails)
# For Gmail: Enable 2FA and create an App Password
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
